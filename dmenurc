#!/bin/bash

# File to be sourced blenever dmenu is run.
source ~/.colors/currentvars # Get the colors we'll need from the theme

DMENU="dmenu -i -h 13 -nb $bg -nf $fg -sb $bg -sf $bl -fn bitocra13"
DMENUR="dmenu_run -i -h 13 -nb $bg -nf $fg -sb $bg -sf $bl -fn bitocra13"
