---
- hosts: targets
  remote_user: jamie
  vars:
    software_list:
      - bash
      - git
      - tmux
      - vim
      - htop
      - curl
      - zsh
      - ranger
      - netcat
  tasks:
    - include: tasks/install.yml
