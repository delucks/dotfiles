### tmux config
### Modified 2014-08-18

# Misc Settings
set -g base-index 1
set -sg escape-time 0
setw -g pane-base-index 1
set -g remain-on-exit off
set -g visual-activity off
set -g default-terminal "screen-256color"
set -g set-titles on
set -g set-titles-string "#W [#I]: tmux"

# Keybindings
set-option -g prefix C-a
unbind-key C-b

# Session settings
set-window-option -g allow-rename on
set-window-option -g automatic-rename on
setw -g aggressive-resize on
setw -g monitor-activity on
bind-key R source-file ~/.tmux.conf ; display 'Reloaded'

unbind-key ,
unbind-key h
unbind-key j
unbind-key k
unbind-key l

unbind-key n
bind-key Tab next-window

bind-key , last-window
bind-key -r h select-pane -L
bind-key -r j select-pane -D
bind-key -r k select-pane -U
bind-key -r l select-pane -R

# Statusbar Settings
set -g status-fg black
set -g status-bg default

set-window-option -g window-status-fg default
set-window-option -g window-status-bg default
set-window-option -g window-status-attr default
set-window-option -g window-status-format '#[fg=yellow]#[bg=black] #I #[fg=default]#[bg=black] #W #[bg=default]'

set-window-option -g window-status-current-fg default
set-window-option -g window-status-current-bg default
set-window-option -g window-status-current-attr default
set-window-option -g window-status-current-format '#[bg=black]#[fg=red] #I #[bg=red]#[fg=default] #W #[bg=default]'

#set-window-option -g window-status-bell-fg red
#set-window-option -g window-status-content-fg blue
set-window-option -g window-status-activity-bg red
#set-window-option -g window-status-bell-fg red

set -g pane-border-bg default
set -g pane-border-fg red

set -g pane-active-border-bg default
set -g pane-active-border-fg black

set -g status-position top 
set -g status-justify centre
set -g status-left-length 0
set -g status-right-length 0
bind-key -r o display "#H"
set -g status-left ''
set -g status-right ''
# look ma, hostname specific configuration
if 'test "#h" = "triton"' 'set -g status-left "┌[#I]"'
